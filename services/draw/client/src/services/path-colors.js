const pathColors = [
    '#000',
    '#fff',
    '#f00',
    '#0f0',
    '#00f',
];

export function defaultPathColor() {
    // TODO could return default color
    return pathColors[0];
}

export default pathColors;